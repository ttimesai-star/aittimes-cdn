# aittimes-cdn — STRICT WHITELIST
# Этот публичный репо — ТОЛЬКО для медиа-файлов автопостинга @aittimes IG.
# Секреты, скрипты, конфиги, токены — НИКОГДА.

# Запрещаем всё по умолчанию
*

# Разрешаем явно нужное
!.gitignore
!README.md
!*.png
!*.jpg
!*.jpeg
!*.mp4
!*.webp

# Запрещаем подпапки (чтобы случайно не пушнуть workspace)
*/

# Если когда-то нужны подпапки — явно разрешать (например "!subfolder/")
